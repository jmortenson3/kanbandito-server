const { createTask, deleteTask, getTasks, updateTask } = require('./tasks');

test('retrieves a single task', () => {

});

test('retrieves a list of tasks', () => {

});

test('creates a task', () => {

});

test('deletes a task', () => {

});

test('updates a task', () => {

});